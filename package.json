{
  "name": "ai-lead-recovery",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-tenant AI Lead Recovery Platform (monorepo)",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
